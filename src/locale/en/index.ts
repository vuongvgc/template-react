import { MessageFormatElement } from 'react-intl';

import common from './common';

const en: Record<string, string> | Record<string, MessageFormatElement[]> | undefined = {
  ...common,
};
export default en;
