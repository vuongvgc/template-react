import { MessageFormatElement } from 'react-intl';

import common from './common';

const vi: Record<string, string> | Record<string, MessageFormatElement[]> | undefined = {
  ...common,
};
export default vi;
